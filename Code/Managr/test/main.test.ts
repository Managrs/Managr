import { it,expect,describe} from "vitest";

//passing test to setup
describe('passer', () => {
    it('Should',() => {
        expect(1).toBeTruthy();
    });
});

//Check that imports are correct

//Check app uses are added